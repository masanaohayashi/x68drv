/// X68000 disk image parsing and Human68k filesystem (read-only for v0.1).
///
/// This module intentionally contains no emulator code. Format knowledge
/// comes from design docs and offline analysis of reference emulators.
public enum X68Core {
    /// Library semantic version (independent of the app bundle version).
    public static let version = "0.1.2"
}
